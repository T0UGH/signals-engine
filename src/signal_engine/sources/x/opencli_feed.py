"""OpenCLI-based X feed source (stub for Phase 1)."""
from typing import Any


def fetch_opencli_feed(limit: int = 100) -> list[dict[str, Any]]:
    """Fetch X feed via opencli (stub — returns empty list)."""
    # TODO: invoke opencli timeline --limit N
    return []
